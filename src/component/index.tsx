import { Box } from "@mui/material"

export const Dashboard = () => {
  return (
    <Box component="section"
      sx={{ p: 2, border: '2px solid green', textAlign:'center', borderRadius: '5px' }}
    >
      Welcome to The CoreSteps
    </Box>
  )
}